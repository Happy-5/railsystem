require 'spec_helper'

describe Railsystem do
  it 'has a version number' do
    expect(Railsystem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
